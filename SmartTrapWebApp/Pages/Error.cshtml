@page
@model ErrorModel
@{
    ViewData["Title"] = "エラー";
}

<h1 class="text-danger">エラー</h1>
<h2 class="text-danger">要求中にエラーが発生しました</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>リクエスト ID:</strong> <code>@Model.RequestId</code>
    </p>
}

<h3>開発モード</h3>
<p>
    <strong>開発モード</strong> に変更すると詳細を表示します
</p>
<p>
    <strong>開発モードは本番サーバで有効にすべきではありません</strong>
    開発モードは環境変数の<strong>'ASPNETCORE_ENVIRONMENT'</strong>を'Developement'に設定しアプリケーションを再起動してください
</p>
